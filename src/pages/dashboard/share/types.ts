export type SummaryItem = {
  key: string;
  label: string;
  color: string;
  value: number | string;
};

export type StatusMeta = {
  label: string;
  color: string;
  bg: string;
};

export type FilterOption = {
  value: string;
  label: string;
};
